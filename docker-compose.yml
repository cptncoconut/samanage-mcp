services:
  samanage-mcp:
    build: .
    container_name: samanage-mcp
    restart: unless-stopped
    env_file: .env
    environment:
      HOST: 0.0.0.0
      PORT: 8765
    ports:
      - "8765:8765"
