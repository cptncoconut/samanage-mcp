services:
  samanage-mcp:
    build: .
    container_name: samanage-mcp
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 8765
      SAMANAGE_API_TOKEN: ${SAMANAGE_API_TOKEN}
    ports:
      - "8765:8765"
